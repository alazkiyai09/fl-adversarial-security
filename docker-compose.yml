version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
  dashboard:
    build: .
    command: ["python", "-m", "streamlit", "run", "src/dashboard/app/main.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
    ports:
      - "8501:8501"
